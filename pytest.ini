[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Only collect from tests/. Without this, a bare `pytest` from the repo root
# also picks up scripts/ -- where the capability probes construct live API
# clients at import time.
testpaths = tests
